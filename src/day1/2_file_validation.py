
"""
Note:
3 steps of the validation triad
    1. Existence check => If not found the file the program will crash, so we use os.path.exits()
    2. Is empty check => Has a file but empty content in it, so we use os.path.getsize()
    3. Extension check => Is endWith correctly?
"""